<%= form_for(@card_highlight) do |f| %>
  <% if @card_highlight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_highlight.errors.count, "error") %> prohibited this card_highlight from being saved:</h2>

      <ul>
      <% @card_highlight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br>
    <%= collection_select(:card_highlight, :card_id, Card.all, :id, :name, :prompt => true, class: 'ui dropdown') %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>