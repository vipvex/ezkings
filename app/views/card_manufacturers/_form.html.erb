<%= form_for(@card_manufacturer) do |f| %>
  <% if @card_manufacturer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_manufacturer.errors.count, "error") %> prohibited this card_manufacturer from being saved:</h2>

      <ul>
      <% @card_manufacturer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
